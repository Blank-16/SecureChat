# Used by docker-compose.yml for the Postgres container and the
# server's DATABASE_URL. Set a strong value before running
# `docker compose up` in any shared or production environment.
POSTGRES_PASSWORD=changeme
